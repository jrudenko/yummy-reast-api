const {
  addFavoriteController,
  getFavoriteController,
  deleteFavoriteController,
} = require('./favoriteControllers');

module.exports = {
  addFavoriteController,
  getFavoriteController,
  deleteFavoriteController,
};
